require "spec_helper"

describe ActiveRecordPostgresDumpSchemas do
  it "has a version number" do
    expect(ActiveRecordPostgresDumpSchemas::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
